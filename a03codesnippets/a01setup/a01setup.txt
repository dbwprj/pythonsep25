creating the virtual environment.

python3 -m venv venv

source venv/bin/activate

pip install package-name

list of all libraries needed in the project
pip freeze > requirements.txt

pip install -r requirements.txt

pip install pipenv
pipenv install

Set up .gitignore
